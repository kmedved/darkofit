# B-archive v2 result heading erratum

The create-only note at `benchmarks/barchive_v2_result.md` inherited the
heading “Wave 3 B-archive v1 result.” Its correct heading is “Wave 3 B-archive
v2 result.” The authoritative JSON already carries the correct identity,
`wave3_barchive_v2_20260721`.

This erratum changes only that Markdown heading. It does not modify the frozen
evidence, the `4.152525` median effective archive-to-single result, or the
`close_barchive_nominate_fused_lane_dispatch` disposition.
