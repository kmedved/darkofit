# T7b automatic-depth fresh Tier-D v3 fillability enumeration v2

_Prospective successor to
`t7b-automatic-depth-fresh-tier-d-v3-enumeration-v1-20260723`. No feature
matrix, target value, model fit, or candidate/control outcome was accessed by
v1._

Enumeration identity:
`t7b-automatic-depth-fresh-tier-d-v3-enumeration-v2-20260723`.

V1's repository-history scanner received `git grep` paths in
`<40-character revision>:<tree path>` form but compared the entire string to
its narrow P1 disclosure allowlist. All 40 identities therefore self-matched
their own registry declarations and were rejected before any resource load.
The immutable v1 artifact and create-only failure record preserve that result.

V2 changes only path normalization: a valid 40-hex revision prefix and its
single separator are removed before applying the same disclosure allowlist.
The allowlist itself, 40-identity candidate pool, exact DarkoFit and
ChimeraBoost history checks, fingerprint catalogs, loadability checks,
splits/branches, target restrictions, and all non-claims from
`t7b_automatic_depth_fresh_tier_d_v3_enumeration_protocol.md` remain
unchanged.

The v2 harness and this protocol must be committed and published before the
v2 create-only enumeration runs. V1 loaded no dataset, so v2 remains the first
resource audit under P1-v3 and no fresh confirmation inspection has been
spent.
